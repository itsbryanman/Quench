[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "quench-native"
version = "0.1.0"
description = "Rust acceleration module for Quench"
requires-python = ">=3.10"

[tool.maturin]
module-name = "quench_native"
manifest-path = "Cargo.toml"
